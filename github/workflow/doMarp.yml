name: Build
on:
  push:
    branches:
      - main
      - master

jobs:
  BuildAndPublishPage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      
      - name: checkout
        uses: actions/checkout@v3
      
      - name: build page
        run: |
          npm i -g @marp-team/marp-cli
          marp --html --allow-local-files --output index.html index.md

      - name: commit changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          skip_fetch: flase
          skip_checkout: false
          commit_message: 'Build page'
          file_pattern: index.html
